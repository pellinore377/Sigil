# Current work

Active: web messaging candidate runs through the existing Rust client in Wasm, with OPFS storage and a WebCrypto-wrapped key. `/messenger` opens messaging; `/` does so after setup; `/admin` remains administration. Wide screens have a persistent conversation pane. Wasm assets are precompressed; successful content-hashed binaries are immutable, while entry points and private responses remain uncached. Shared text/SigilText composition, request consent, device/contact QR flows, account themes and sign-out are connected. Browser adapters now cover encrypted attachment staging/transfers/forwarding, camera capture, voice recording with captions, media playback/save/viewers, profile photos, recovery, account-access state, collections, service cards and restricted MathML. `/preview` is an explicitly local-only design workbench.

Browser acceptance: encrypted multi-chunk files preserve drafts across reload and decrypt byte-for-byte in another profile; forwarding publishes through the queued transfer worker. Camera/microphone capture, cancellation, profile photo normalization/publication/removal, history restoration and formula rejection checks pass. Actual Compose file selection, captions and sending passed visual inspection. Recovery forms are shared with Android; confirmation tests and Android compilation pass. Isolated Chromium profiles over synthetic HTTPS passed enrollment, encrypted delivery/decryption, reload persistence, QR/emoji device linking, revocation/erasure and interrupted-erasure recovery. Actual UI composition persists messages. Browser JavaScript exceptions are normalized before existing Kotlin error handlers; fresh sign-in and persisted composition pass with no WebGPU adapter. Camera tracks start/stop; dedicated QR pages keep the preview clear of instructions and controls. Navigation releases storage ownership; oversized commands and invalid SSO completion proofs fail. Server library: 143 passed, plus the cache-policy regression; native network: 10 passed; shared decoder/form/Create checks and Android compilation pass; browser-target Clippy passes. Browser SSO callback routing/proof tests pass, but real-provider browser sign-in remains untested. These checks do not certify security.

Open browser work: compatible calling transport, real-provider notification delivery, maps, broader media viewers, full account-access/SSO acceptance, cross-browser/accessibility/performance acceptance, container verification and production deployment. Browser uploads use encrypted chunks up to 1 GiB; Blob previews and fallback saving are capped at 128 MiB; browsers with File System Access stream larger authenticated files to a chosen destination. Byte-exact streaming and preserving the original destination after failure pass. Device-local encrypted wallpapers, image zoom and browser-local date ordering are connected. Admin has a visible Messages button; real browser navigation to /messenger passes. Shared composer acknowledgement tests preserve newer structured drafts and builders being edited. Browser push has a small separate worker without messaging keys; native proof/restart/replacement/disable checks, real Chromium subscription/reuse/unsubscribe and injected wake display pass. Real provider confirmation delivery has not passed. An authenticated data-channel carrier interoperates with native RTP for encrypted fragmented audio/camera/screen payloads in tests; browser capture/playback and keyframe feedback remain unconnected. GitHub container build, smoke checks and GHCR publication pass. Unsupported platform controls are hidden or disabled. One tab owns each browser device. The web host remains trusted to deliver client code. No phone, home-server or VPS changes were made for this candidate. The GitHub image workflow builds the web bundle and publishes revision/latest tags.

Composer revisions: above-input tools now use consistently sized squircles and labels, direct common actions, a compact wide-screen grid, optional Create filters, and a single editor/preview switch. Voice drafts retain a caption field. Shared panels compile for Android; their latest physical-device and aesthetic acceptance remain open.

Research assessment: source confirms shared native authenticated/anonymous transport, globally exposed mailbox cursors and server-readable profiles. Prioritize measured transport separation, recipient-bound cursors, encrypted profiles and release verification. Key transparency needs independent witnesses and failure policies; privately authorized delivery requires a versioned consent/retry redesign. Browser Fetch pools are browser-controlled. OHTTP and mixnet work stays a separate prototype; preserve instant messaging by default. No privacy-protocol changes were made. Stale security/recovery schema references were corrected.

Material follow-up: Android and browser share presentation code and Rust rendering/flight calculations. Browser WebGPU renders transparently in a standalone Chromium check. Flight calculation uses a bounded worker; GPU frames coalesce instead of accumulating. Integrated SwiftShader load lost the browser graphics context, so software adapters use the lightweight fallback. The software fallback displays stored dice/coin/picker results in the real messenger without losing the graphics context. Hardware-backed messenger motion, scrolling and interaction acceptance remain open. Rust material tests (13), Wasm Clippy and Android compilation pass. Personalized designs currently apply the viewer’s settings; sending designs, native desktop rendering and standalone d% input remain open. Native shapes include d4/d6/d8/d10/d12/d16/d20/d24/d30; d100 uses a percentile pair. d6 keeps pips. Material textures and appearance controls remain in place. Android performance and the approved design implementation remain pending as detailed below. Android `0.1.0-alpha.13` (code 14) and server image `59f13aa`/`latest` were published from `main`; the APK retains its signing certificate. Container upgrade/restart/restore acceptance passed. Current home-server deployment has not been rechecked.

Android alarm cancellation remains open: deleting a timer must remove its scheduled platform identifier. Rebuild/cancellation behavior still needs diagnosis and phone testing.

## Android

Implemented: shared stationary headers and footer/composer transitions; inbox/search/notes/collections; request states and consent gating; timeline receipts, reactions, reply/thread swipes and message menus; account/profile/device/recovery controls; private account themes with device opt-out; colors, typography, layout and motion/media appearance pages. Newsreader or Google Sans Flex applies to ordinary text, with Google Sans Code for code. Accents affect outgoing bubbles, controls, cursors and selections. Reduced motion and GIF autoplay settings are connected.

Encrypted attachment drafts and captions survive restart and preview before explicit Send. Camera capture stays inside the composer with Retake/Use photo and retry. Voice supports pause/resume, waveform, seeking, captions and expanded playback. Images support EXIF orientation and region-based zoom; encrypted GIFs respect lifecycle/autoplay. Videos have expanded playback and captions. PDFs have page navigation and zoom; text/contact files have literal paging; CSV/TSV/spreadsheets have sheet/cell navigation and copy; STL/3MF have geometry rotation. These bounded viewers run in an isolated Android process without network permission or encryption keys and receive authenticated read-only streams without plaintext disk staging. Spreadsheet values are not recalculated; spreadsheet formatting/charts and 3MF textures are omitted. Remaining formats have file details/external opening; Office document/slide viewers remain unimplemented on Android.

Locations support once/live/pin, durations, coordinate fallback, avatar markers, expanded maps and Stop. GPS starts only for explicitly authorized sharing. Live sharing uses a visible foreground notification; stale/expired markers stop pulsing. A linked device's pending Stop survives restart without gaining sampling authority. Real background GPS acceptance remains pending.

Canonical rich text retains UTF-16 presentation ranges, links, named colors, spoilers and scratch actions. Code has language labels, bounded highlighting, exact Copy and expanded wrap/scroll viewing. Concealed code is excluded from those controls. Tables have bounded previews, sticky expanded headers, horizontal scrolling, local numeric sorting and protected cell/row/table copy. Recipes have local ingredient checks, step navigation, optional screen-awake mode and Rust serving adjustments that preserve ambiguous/concealed quantities. Charts render six types with exact-value selection, pinch/pan/fit, numbered details, visibility toggles and protected data copy. Diagrams have bounded overviews, visible-node rendering, pan/zoom/fit, directed/labeled edges, node lists/focus, branch controls and ordered timelines. Utility cards provide exact calculation/conversion copy, committed random results, QR reveal/scan controls, swatches, shortcuts, ratings/progress and quotes. Android formulas render bounded Rust MathML with WebView scripting, network, file and content access disabled; ASCII art uses the code viewer. Translation/definition/weather snapshots retain attribution, protected copy, original text, extra senses, local weather units, dated forecasts and hourly curves; opening them makes no provider request. Pronunciation audio currently opens externally.

Shared contact cards have expanded details and protected vCard copying without remote avatar requests. Their Message action checks the shared account reference against the directory before opening the normal consent flow; it never automatically sends or accepts a request. Stable identity details remain secondary. Rust and phone tests cover explicit invocation, stale account claims, view-once refusal and unchanged request/admission state.

The live editor projects named inline styles through the Rust parser, with grapheme-bounded gradients, escaped punctuation, Unicode editing and undo. New text sends use canonical formatting/redaction; plain messages remain compact and legacy pending commands keep their behavior. Captions retain their existing Markdown presentation. Formatting offers a source toggle and a code-block builder with optional language, saved draft, canonical highlighted preview/Copy/expanded viewing and explicit Send. Rust selects safe fences so embedded backticks and SigilText remain literal; line endings and Unicode follow the existing text normalization. Desktop restoration, durable client storage and 18 physical code/composer/rich-text checks pass. Inline messages reopen through on-demand, round-trip-checked source reconstruction; unsupported blocks/mentions refuse source editing rather than dropping their metadata. Reveal modifiers remain visible as source in the editor; authored animations use static formatting plus an indicator.

Inline authored effects now use Rust parameters and bounded drawing with fixed text layout. New messages play once; history stays settled; message actions replay locally. Playback pauses offscreen/background and respects reduced motion and disabled message effects. Flip retains upside-down glyphs in its settled state. ASCII characters animate separately; non-ASCII words/emoji stay together. Concealed spans remain static; oversized animation spans/layers fall back to static text. Six desktop playback/rendering tests, the physical ten-effect/replay test and full physical content acceptance pass. Charts now share this lifecycle: bars grow from zero, lines draw, pie/donut slices reveal and scatter markers settle. Six-type desktop rendering/replay checks and two physical chart tests pass, including the timeline menu and unchanged values. Randomizers now render bounded volumetric dice/coins and choice/number reveals using stored results. Standard polyhedra, face labels, replay, reduced motion and lifecycle behavior pass desktop and physical checks. Card text actions inherit their surface contrast; full desktop and physical content acceptance pass.

Reminder/timer builders show a Rust-resolved date/duration before Send. Numeric dates follow the sender locale, adjusted daylight-saving times include their UTC offset, and the confirmed absolute date and timezone survive sending. Rust, desktop and full physical content acceptance pass, including the stored reminder timestamp.

Randomizer forms support dice groups, literal choices, inclusive number ranges and coin flips within the composer. Table creation has column/row editors, bounded literal fields, animated row navigation and an explicit preview/Send step. Both use one Rust validation/escaping bridge; editing generates no random results. Stored results and durable acknowledgements survive reopening. Full physical content acceptance and 17 randomizer/shared-form/help checks pass, followed by four table/randomizer/viewer checks. Desktop table restoration and oversized-paste rejection pass; single-line pasted breaks stay within their field.

SigilText help is searchable locally from Create or `help::`, with categories, literal examples, keyboard navigation and explicit copy/share actions. Sharing uses the canonical Rust cheatsheet and preserves example syntax without executing it. Rust, desktop, two physical help tests and adjacent composer/navigation tests pass; contextual completion and example insertion remain open.

Create exposes all available tools directly, with filters for Plan & Organize, Ask & Decide, Data & Visualize, Share & Encode and Reference & Services. Help and intent search remain separate. New physical acceptance is pending.

Remaining implementation: complete graphical round-trip editing and mention completion, full contact import, remaining media formats and viewers, map-composer address search, map style catalogs/download/setup, and remaining Admin service controls. Desktop messaging still lacks a storage adapter. Admin appearance remains browser-local; native account appearance syncs privately and wallpapers remain device-local. Alarm notifications, self-service password changes and broader accessibility/audio-routing/battery acceptance remain open.

## Performance and delivery

Validated HTTPS discovery and connection pools survive native store reopening, with bounded lifetime and credential/trust-root isolation. Opaque hardware handles are cached while each access still unwraps the key; decrypted keys are not cached. Outbound messaging and call work precede inventory/recovery maintenance. UI timeline reads bypass network work and account refresh. Sync scheduling preserves backoff/Retry-After and uses faster bounded call-setup polling. Android delayed maintenance now has a separate job and retains the earliest requested time, so it cannot overwrite an immediate wake-up. A physical regression reproduced the replacement before the fix; scheduling, push lifecycle and synthetic sync/proof tests pass afterward.

Completed ciphertext frames are assembled into bounded per-stream queues before authenticated decoding. This prevents video starvation under the receive budget. Every frame retains authority, signature, AEAD and replay checks. RTP reordering, loss/keyframe recovery, rotated video scaling and audio priming are connected. All four physical synthetic call modes pass, including group continuation after creator departure/rekey.

Synthetic OnePlus measurements: hardware unwrap p95 49→37 ms; combined media send p95 33→28 ms, audio 18 ms; local direct-call answer→connection about four seconds. A local timeline read completed in 73 ms while the command queue was locked. These do not establish real WAN delivery latency or subjective call quality. Two-phone retesting remains necessary.

Android FCM and UnifiedPush adapters use encrypted Rust token/proof/rotation state. Explicit disabled/UnifiedPush choices survive Google token callbacks. Notifications run before bulk maintenance. The standard APK obtains validated public Firebase options from its authenticated server; per-server builds are no longer needed. Private service-account keys stay on the server. Startup reconciles durable opt-out; project changes reject old callbacks and require a cold restart. Four physical lifecycle checks and synthetic UnifiedPush proof delivery pass. Real Firebase configuration and credentials are not supplied. Live FCM, idle-device ringing and provider-choice polish remain pending; normal-priority wake-ups do not establish prompt Doze delivery.

## Validation

Full physical content acceptance passes through encrypted transfers/playback, push registration/proofs, recovery publication, replacement-history restore and device sign-out/removal. Server-dependent checks now finish before standalone UI checks so the fixed fixture lifetime does not expire during unrelated rendering tests; no tests or assertions were removed. The complete run covers PDF/native viewer isolation, malformed input, spreadsheet cached values, dashboard/contact details, diagram, utility and service cards, messaging/revision/composer checks, Create navigation, randomizer/table builders, rich text, tables, recipes, charts, layout, timeline isolation/performance, QR linking and location UI. Tests use synthetic content in the isolated acceptance app. Service cards cover original/reveal/copy, extra definitions and weather units/hour navigation without resolution commands. Utility tests verify exact copying, QR concealment and restricted formula rendering. Diagram tests cover six types, including branch controls, edge-label placement and large-hierarchy navigation.

Full RTC release suite: 387 passed/7 ignored, followed by the expanded code-builder persistence test. Server release tests: 238 passed, including migration/restore and Admin/device separation for Firebase options. Synthetic downgrade fixtures remove the schema-34 table before rebuilding. The last unoptimized client suite passed 371/11 ignored before the visual-card additions; four additional unoptimized exclusions are cryptographic boundary cases exercised in release. Other ignored entries are separately invoked acceptance/load tests. Federation fixtures consume POST bodies and honor bounded Retry-After. Rust text (57 plus location), core (15), Clippy, shared UI, Android and Wasm checks pass; portable and Linux-worker decoder tests pass. Protocol 16 and crypto 99 tests passed, followed by the two push protocol tests; one crypto reference-generator test is separately invoked. Push-registration investigation reproduced an unnecessary disable request that imposed the server's 60-second registration cooldown. Already-disabled states now settle locally after reconciliation; initial opt-out and missing-endpoint regressions pass, while actual replacement still observes the cooldown. Full phone acceptance passes afterward. Timeout diagnostics expose scheduling state without tokens. Workspace formatting still reports pre-existing differences. Commands are in the README.

Synthetic server benchmark: with 4 CPUs/8 GiB, 50 accounts, 20 devices and concurrent 160 MiB uploads, durable feedback was 2.3 ms p95 and recipient decryption 119 ms p95 under 50 ms HTTPS RTT. Two-server recipient decryption was 164 ms p95. A 100,000-message scan took 17.1 s; a bounded search page took 16.2 ms p95. A warmed Android scroll sample measured 7.5 ms median/15.6 ms p95; this is not a release/battery benchmark.

## Backend and deployment

Backend/shared Rust roadmap items #1–#12 are implemented. Local server schema 34, native schema 77, attachment cache 5. Production remains schema 33. The container includes the Compose/Wasm setup wizard and current Admin interface, Argon2id passwords, OIDC linking/transitions, account/group administration and separate user-password policy. Advanced maintenance/service controls remain APIs. GHCR `latest` and revision tags support deployment; TCP 8080 serves HTTP and UDP 34780 serves calls. Existing server schema 33 remains compatible with alpha.12. Obtainium/update signing instructions are in the README.

The Admin overview now samples real diagnostics into bounded session-local queue charts, with missing-history/gap/stale states, numeric navigation, storage proportions and maintenance failures. Logical payload sizes are distinguished from database allocation. Browser production builds and shared dashboard tests pass; deployed-browser visual acceptance remains pending.

Admin → Server → Notifications supports Google service-account import/replacement, public Android `google-services.json` import, UnifiedPush contact settings, disabling providers and explicit signing-key rotation. Changes preserve egress exceptions and exact revisions; forms clear after saving. Android options are bound to the enabled project/generation and invalidated by restore. Four desktop UI tests, four browser adapter tests and 15 server push tests pass, along with the production Wasm build. Saving configuration does not establish provider delivery.

Directory-bound first contact requires accepted contact consent; independent fingerprint comparison is optional. Unexpected identities need trusted-device endorsement or explicit replacement approval. Contact QR codes authorize a bound, single-use request. Accidental declines can be reversed by the recipient before expiry; senders cannot override blocks/declines. Account recovery revokes old devices and requires replacement consent. History recovery never restores live identity/session keys or peer approval. Profile sharing is server-visible metadata, separate from encryption identity.

Two-server messaging/group/history/file/call acceptance and container schema 33→34 upgrade, restart/retries, backup/import/restore, push reset and revocation pass. Pending encrypted-upload compatibility acceptance remains open. Opt-in bounded ciphertext logging excludes plaintext, credentials and private keys.

## Security limits

[Audit.md](Audit.md) records the backend Codex review and four independent Claude batches. Confirmed restore-journal, quota, maintenance-artifact and federation-counter defects were fixed. That audit predates browser/mobile additions; later code has self-review and regression tests. No security certification is claimed.

Independent HMAC/HKDF expectations cover 36 initial Triple Ratchet packets; OpenSSL/libsodium PQXDH fixtures pass. Four ASan fuzz targets completed about 17.9 million executions without a product crash. These do not prove composition, later-epoch/post-compromise security, RaptorQ healing bounds or target-machine constant-time behavior. Gaps beyond the skipped-key budget can stall a session; automatic reset is refused. Mailbox cursors expose aggregate activity, and group credential issuance trusts the homeserver. Erasure tests cannot guarantee physical media erasure.

Three dependency advisories and one build-tool warning remain in [Security.md](Security.md); `cargo audit` is not clean. [plan.md](plan.md) is unchanged. [Design.md](Design.md) remains the UI contract, including requirements not yet implemented.
