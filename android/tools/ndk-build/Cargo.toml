# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
rust-version = "1.60"
name = "ndk-build"
version = "0.10.0"
authors = ["The Rust Windowing contributors"]
description = "Utilities for building Android binaries"
homepage = "https://github.com/rust-mobile/cargo-apk"
documentation = "https://docs.rs/ndk-build"
readme = "README.md"
keywords = [
    "android",
    "ndk",
    "apk",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/rust-mobile/cargo-apk"

[dependencies.dirs]
version = "4"

[dependencies.dunce]
version = "1"

[dependencies.quick-xml]
version = "0.26"
features = ["serialize"]

[dependencies.serde]
version = "1"
features = ["derive"]

[dependencies.thiserror]
version = "1"

[dependencies.which]
version = "4"

# SIGIL PATCH: the `<service>`/`<receiver>` metadata test in `manifest.rs`
# parses the same TOML shape `cargo-apk` reads out of a `Cargo.toml`.
[dev-dependencies.toml]
version = "0.5"
